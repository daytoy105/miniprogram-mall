<!--index.wxml-->
<view class="container">
  <!-- <view class="user">
    <image src="{{userInfo.avatarUrl}}" class="avatar"></image>
    <text class="name">{{userInfo.nickName}}</text>
  </view> -->
  <canvas canvas-id="user-info" style="width:100%;height:100%" ></canvas>
  <view class="pop" style="display:{{pop_url?'block':'none'}}">
    <image src="{{pop_url}}"></image>
  </view>
</view>